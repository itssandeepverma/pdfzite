import type { ToolInfo } from './types'

const imageCrop: ToolInfo = {
  id: 'image-crop',
  label: 'Crop image',
  tagline: 'Keep only the focus.',
  description: 'Crop by percentage bounds to isolate the area you need.',
  category: 'image',
}

export default imageCrop
