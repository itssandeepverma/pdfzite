import type { ToolInfo } from './types'

const pageNumbers: ToolInfo = {
  id: 'page-numbers',
  label: 'Add page numbers',
  tagline: 'Number every page.',
  description: 'Adds centered page numbers in a single pass.',
  category: 'pdf',
}

export default pageNumbers
