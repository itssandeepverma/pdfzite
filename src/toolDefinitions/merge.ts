import type { ToolInfo } from './types'

const merge: ToolInfo = {
  id: 'merge',
  label: 'Merge PDFs',
  tagline: 'Stack multiple PDFs into one.',
  description: 'Reorder and unify files locally with no uploads and no size limits.',
  category: 'pdf',
}

export default merge
