import type { ToolInfo } from './types'

const countText: ToolInfo = {
  id: 'count-text',
  label: 'Word and char count',
  tagline: 'Quick text metrics.',
  description: 'Counts words, characters, and lines live as you type.',
  category: 'text',
}

export default countText
