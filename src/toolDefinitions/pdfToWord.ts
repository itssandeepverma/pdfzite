import type { ToolInfo } from './types'

const pdfToWord: ToolInfo = {
  id: 'pdf-to-word',
  label: 'PDF to Word',
  tagline: 'Extract text into DOCX.',
  description: 'Pulls text content locally and drops it into a .docx document.',
  category: 'pdf',
}

export default pdfToWord
