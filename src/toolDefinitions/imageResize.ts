import type { ToolInfo } from './types'

const imageResize: ToolInfo = {
  id: 'image-resize',
  label: 'Adjust pixels',
  tagline: 'Exact width and height.',
  description: 'Set pixel dimensions directly for banners, documents, or thumbnails.',
  category: 'image',
}

export default imageResize
