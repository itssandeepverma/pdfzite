import type { ToolInfo } from './types'

const codeDiff: ToolInfo = {
  id: 'code-diff',
  label: 'Code diff checker',
  tagline: 'Spot additions and removals.',
  description: 'Paste two snippets and see highlighted differences.',
  category: 'code',
}

export default codeDiff
