import type { ToolInfo } from './types'

const organizePdf: ToolInfo = {
  id: 'organize-pdf',
  label: 'Organize PDF pages',
  tagline: 'Change page order.',
  description: 'Load a PDF, reorder pages with arrows, then export.',
  category: 'pdf',
}

export default organizePdf
