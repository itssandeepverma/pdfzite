import type { ToolInfo } from './types'

const split: ToolInfo = {
  id: 'split',
  label: 'Split PDF',
  tagline: 'Slice by page range.',
  description: 'Extract exact pages with simple ranges like 1-3,7.',
  category: 'pdf',
}

export default split
