import type { ToolInfo } from './types'

const imageReduce: ToolInfo = {
  id: 'image-reduce',
  label: 'Reduce image size',
  tagline: 'Scale down and compress safely.',
  description: 'Resize with gentle quality controls to avoid blur.',
  category: 'image',
}

export default imageReduce
